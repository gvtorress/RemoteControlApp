{
    "name": "remote-control-server",
    "version": "1.0.0",
    "description": "server that gets the command from the app and runs a command line or a python script, depending on what was sent by the app",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "dev": "nodemon index.js",
        "build": "nexe index.js -r src/routes/main.routes.js -t x64-14.15.3",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Gabriel Torres",
    "license": "ISC",
    "dependencies": {
        "child_process": "^1.0.2",
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "morgan": "^1.10.0"
    },
    "devDependencies": {
        "nexe": "^4.0.0-beta.19"
    }
}
